# Matriz de rastreabilidade

Liga regras de negócio (RN) e requisitos das capacidades a endpoints, tarefas de `tasks.md` e testes que comprovam a implementação (nomes de classe reais, pós-implementação em 2026-08-12).

| RN / Requisito | Capacidade | Endpoint(s) | Tarefas | Teste que comprova |
|---|---|---|---|---|
| RN01 — CNPJ válido e existente | gestao-corretoras | `POST /corretoras` | 3.4, 5.2, 6.1, 7.1 | `CnpjUtilsTest`; `CorretoraServiceTest#cnpjInvalidoNaoConsultaProvedores`; `BrasilApiCnpjAdapterTest#cnpjNaoEncontradoLancaExcecaoEspecifica`; `CorretoraControllerTest#cnpjInvalidoRetorna400` |
| RN02 — cadastro não aceita preenchimento manual dos dados empresariais | gestao-corretoras | `POST /corretoras` | 6.1 | `CorretoraRequest` não tem campos `razaoSocial`/`nomeFantasia`/`situacaoCadastral` (só podem vir de `DadosCnpj`); `CorretoraServiceTest#registrarComSucessoPersisteCorretora` confirma que esses campos vêm do mock de `CnpjDataPort` |
| RN03 — rejeitar instituição não validada | gestao-corretoras, validacao-instituicao-financeira | `POST /corretoras` | 5.4, 6.1 | `CorretoraServiceTest#instituicaoNaoValidadaRejeitaCadastroSemConsultarEndereco` (422, `enderecoPort` nunca chamado, `save` nunca chamado); `BrasilApiCvmAdapterTest` |
| RN04 — CEP validado externamente | gestao-corretoras, integracao-enderecos | `POST /corretoras` | 3.4, 5.3, 6.1 | `CepUtilsTest`; `ViaCepAdapterTest` (3 testes: sucesso, inexistente, falha infra); `CorretoraServiceTest#cepInvalidoNaoConsultaEndereco` |
| RN05 — ticker confirmado pelo provedor | gestao-acoes, integracao-cotacoes | `POST /acoes` | 5.5, 5.6, 6.2 | `AcaoServiceTest#tickerNaoEncontradoNaoPersisteNada`; `BrapiAdapterTest#resultadoVazioLancaTickerNaoEncontrado`; `TwelveDataAdapterTest#simboloNaoEncontradoLancaTickerNaoEncontrado`; `AlphaVantageAdapterTest#globalQuoteVazioLancaTickerNaoEncontrado` |
| RN06 — roteamento por mercado | gestao-acoes, integracao-cotacoes | `POST /acoes` | 5.6, 6.2 | `CotacaoStrategyResolverTest` (3 testes: BRASIL→Brapi, ESTADOS_UNIDOS→Twelve Data por padrão, ESTADOS_UNIDOS→Alpha Vantage quando configurado) |
| RN07 — unicidade global de ticker | gestao-acoes | `POST /acoes` | 2.3, 4.2, 6.2 | `AcaoRepositoryTest#rejeitaTickerDuplicadoNoMesmoMercado`/`#rejeitaMesmoTickerEmMercadosDiferentes`; `AcaoServiceTest#tickerDuplicadoNaoConsultaCotacao`; `AcaoControllerTest#acaoDuplicadaRetorna409` |
| RN08 — `BigDecimal` para cotação | gestao-acoes | `POST /acoes`, `PUT /acoes/{id}/atualizar-cotacao` | 3.1, 5.5, 5.6 | `Acao.cotacaoAtual` é `BigDecimal` (revisão de código, sem `double`/`float` em nenhum tipo monetário); `BrapiAdapterTest`/`TwelveDataAdapterTest`/`AlphaVantageAdapterTest` confirmam escala 4 com `isEqualByComparingTo` |
| RN09 — timezone/offset da cotação | gestao-acoes | `POST /acoes`, `PUT /acoes/{id}/atualizar-cotacao` | 3.1, 5.5, 5.6 | `Acao.dataHoraCotacao` é `OffsetDateTime`; coluna `TIMESTAMP WITH TIME ZONE` (`V2__create_acao.sql`); adaptadores convertem UTC (`Instant`) ou epoch para `OffsetDateTime` |
| RN10 — nenhuma persistência parcial | gestao-corretoras, gestao-acoes, tratamento-erros-resiliencia | `POST /corretoras`, `POST /acoes` | 6.1, 6.2 | `CorretoraServiceTest#falhaExternaAposEtapasAnterioresNaoPersisteNada`; `AcaoServiceTest#tickerNaoEncontradoNaoPersisteNada` (ambos: `verify(repository, never()).save(any())`) |
| RN11 — manter última cotação válida em falha de atualização | gestao-acoes | `PUT /acoes/{id}/atualizar-cotacao` | 6.3 | `AcaoServiceTest#falhaNaAtualizacaoMantemUltimaCotacaoValida` |
| Unicidade de CNPJ (mesmo com máscara diferente) | gestao-corretoras | `POST /corretoras`, `GET /corretoras/cnpj/{cnpj}` | 2.3, 3.4, 4.2 | `CorretoraRepositoryTest#rejeitaCnpjDuplicado`; `CorretoraServiceTest#cnpjDuplicadoNaoConsultaProvedores`; `CorretoraControllerTest#cnpjDuplicadoRetorna409` |
| Persistência atômica ponta a ponta | gestao-corretoras, gestao-acoes | `POST /corretoras`, `POST /acoes`, `PUT /acoes/{id}/atualizar-cotacao` | 9.2 | `CorretoraFluxoIntegrationTest`; `AcaoFluxoIntegrationTest` (H2 real + MockWebServer, sem mocks de repositório) |
| Listagem paginada de corretoras | gestao-corretoras | `GET /corretoras` | 6.4, 7.1 | `CorretoraControllerTest#listarRetorna200ComPaginacao` |
| Busca de corretora por ID/CNPJ | gestao-corretoras | `GET /corretoras/{id}`, `GET /corretoras/cnpj/{cnpj}` | 6.4, 7.1 | `CorretoraControllerTest#buscarPorIdInexistenteRetorna404`/`#buscarPorCnpjInexistenteRetorna404` |
| Listagem paginada de ações | gestao-acoes | `GET /acoes` | 6.4, 7.2 | `AcaoControllerTest#listarRetorna200ComPaginacao` |
| Busca de ação por ID/ticker (ticker globalmente único) | gestao-acoes | `GET /acoes/{id}`, `GET /acoes/ticker/{ticker}` | 6.4, 7.2 | `AcaoServiceTest#buscarPorTickerInexistenteLancaRecursoNaoEncontrado`/`#buscarPorTickerComSucessoNormalizaEntrada`; `AcaoControllerTest#buscarPorTickerInexistenteRetorna404`/`#buscarPorTickerComSucessoRetorna200` |
| Isolamento de fornecedor (Ports and Adapters) | integracao-dados-empresariais, integracao-enderecos, validacao-instituicao-financeira, integracao-cotacoes | — (interno) | 5.1–5.7 | 6 classes de teste de adaptador com `MockWebServer` (19 testes); revisão de código confirma que `controller`/`model` não importam `integration.*` |
| Timeout / rate limit (429/401) sem loop | tratamento-erros-resiliencia | todos | 5.7, 6.1, 6.2, 6.3 | `feign.client.config.default` em `application.yml`; `BrapiAdapterTest#tokenAusenteLancaExcecaoDeIntegracao`; `TwelveDataAdapterTest#chaveInvalidaLancaExcecaoDeIntegracao`; `AlphaVantageAdapterTest#limiteDeRequisicoesLancaExcecaoDeIntegracao` |
| Nenhuma chamada real a APIs externas na suíte padrão | integracao-cotacoes, tratamento-erros-resiliencia | — | 9.2, 9.3 | Todas as URLs-base são sobrescritas via `@DynamicPropertySource` para `http://localhost:<porta MockWebServer>`; `./mvnw test` executado 2x consecutivas com 74/74 testes verdes |
| Formato de erro centralizado (Problem Details) | tratamento-erros-resiliencia | todos | 8.1 | `GlobalExceptionHandler`; `CorretoraControllerTest`/`AcaoControllerTest` cobrindo 400/404/409/422/502 |
| Rota inexistente retorna 404 (não 500) | tratamento-erros-resiliencia | todos | 8.1 | `CorretoraControllerTest#rotaInexistenteRetorna404`; verificado manualmente com a aplicação real |
| Busca de corretora por CNPJ mascarado (com `/`) na URL | gestao-corretoras | `GET /corretoras/cnpj/{*cnpj}` | 7.1 | `CorretoraControllerTest#buscarPorCnpjComMascaraContendoBarraFuncionaNaUrl`; verificado manualmente (`GET /corretoras/cnpj/44.527.444/0001-55` → 200) |
| Segredos fora do código/log | tratamento-erros-resiliencia, documentacao-observabilidade | — | 2.1, 2.4, 8.2 | `.env.example` sem valores reais; `BrasilApiCnpjAdapter.mascarar(cnpj)` evita logar CNPJ completo em mensagens de exceção |
| Documentação OpenAPI acessível | documentacao-observabilidade | `/swagger-ui.html`, `/v3/api-docs` | 10.1 | Verificado manualmente: app subida localmente, `GET /v3/api-docs` → 200 com todos os paths |
| README reproduzível | documentacao-observabilidade | — | 10.2 | `README.md` revisado; comandos conferem com `pom.xml` e perfis reais |
| Artefatos de apresentação (Postman, diagramas) | documentacao-observabilidade | — | 10.3, 10.4 | `postman/gestao-acoes.postman_collection.json` (JSON válido); `docs/diagramas.md` |
| Banco de dados H2, MySQL e PostgreSQL (seção 6 do enunciado, todos obrigatórios) | — | — | 2.2, 2.3 | H2 (perfil `test`, suíte completa) e PostgreSQL (perfil `dev`, testado ao vivo) confirmados; MySQL (perfil `mysql`) validado via `MysqlMigrationSmokeTest` (H2 `MODE=MySQL`) — sem servidor MySQL real disponível para teste de ponta a ponta, ver limitação em `design.md` |